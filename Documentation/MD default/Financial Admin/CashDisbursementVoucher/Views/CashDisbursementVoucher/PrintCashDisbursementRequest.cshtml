@using Infrastructure.Identity
@inject UserManager<ApplicationUser> UserManager
@model CashDisbursementVoucherVM
@{
    ViewData["Title"] = Resource2.ApprovalDisbursementRequest ?? " ";
    ViewData["Title ListIndex"] = Resource2.CashDisbursementVoucherList ?? " ";
    var boolManagerSignature = Model?.DisbursementRequestSignature != null && !string.IsNullOrEmpty(Model.DisbursementRequestSignature.ImagePath);
    var currentUser = await UserManager.GetUserAsync(User);
    var currentUserName = currentUser?.UserName ?? "Unknown User";
}
<script>
    function printDiv(divId) {
      window.print();
      document.body.innerHTML = originalPage;
      location.reload();
    }
</script>
<style>

    input {
        text-align: center;
    }
    .overflow-table{
     width: 100%;
    overflow-x: auto; 
    -webkit-overflow-scrolling: touch; 
    }
       
        .overflow-table::-webkit-scrollbar {
            height: 4px; 
        }

        .overflow-table::-webkit-scrollbar-thumb {
            background-color: green; 
            border-radius: 2px; 
        }

        .overflow-table::-webkit-scrollbar-track {
            background-color: transparent;
        }
    .container {
        width: 100% !important;
        box-shadow: none !important;
        page-break-inside: avoid;
    }
       th, td {
            border: 1px solid #000 !important;
            padding: 1px !important;
        }
    @@media print {
         .overflow-table{
        width: 100%;
        overflow-x: hidden;
    }
        .print-none,
        .custom-navbar {
            display: none !important;
        }

        body {
            background: #fff !important;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            font-size: 12px;
            margin: 0 !important; 
            padding: 0 !important; 
        }

        * {
            max-width: 100% !important;
        }

        html, body {
            width: 100%;
            overflow: visible !important;
            margin: 0 !important;
            padding: 0 !important;
        }
        #printArea {
            width: 800px !important;
            margin: 0 auto !important;  
            padding: 0 !important;
        }
      
        .row {
            margin-left: 0 !important;
            margin-right: 0 !important;
        }
        .flex-fill {
            flex: 1 1 auto !important;
            min-width: 0 !important;
        }
        table {
            width: 100% !important;
            table-layout: fixed !important;
            box-shadow: none !important;
        border-collapse: collapse !important;
        }

        th, td {
            border: 1px solid #000 !important;
            padding: 1px !important;
        }

         input {
        border: none !important;
        box-shadow: none !important;
        background: transparent !important;
          -webkit-appearance: none;
    } 
    }
</style>
<div class="row my-3 d-flex justify-content-between align-items-center flex-wrap print-none">
    <div class="col-md-6">
        <nav class="d-flex align-items-center justify-content-start" aria-label="breadcrumb">
            <ol class="breadcrumb mb-0 py-2 flex-nowrap">
                <li class="breadcrumb-item">
                    <a href="#">
                        وحدة إدارة الماليات
                    </a>
                </li>
                <li class="breadcrumb-item">
                    <a href="/Admin/CashDisbursementVoucher/Index">
                        @ViewData["Title ListIndex"]
                    </a>
                </li>
                <li class="breadcrumb-item">
                    <a href="#">
                        @ViewData["Title"]
                    </a>
                </li>
            </ol>
        </nav>
    </div>
    <div class="col-md-6">
        <div class="d-flex align-items-center justify-content-end gap-2">
            @if (boolManagerSignature)
            {
                <button onclick="printDiv('printArea')" ; return false;" class="print-btn btn btn-success btn-sm">
                    <i class="fa fa-print me-1"></i> @Resource2.Print
                </button>
            }
            <a id="Backbtn" class="btn btn-cancel-search btn-sm" asp-action="Index">
                @Resource2.Back <i class="fa-solid fa-arrow-left me-1"></i>
            </a>
        </div>
    </div>
</div>
<div class="card">
    <div class="container" id="printArea">
        <div class="d-flex justify-content-between align-items-center flex-wrap  border-bottom py-3  mb-3">
            <div class="d-flex justify-content-between align-items-center flex-fill">
                <div style="text-align:center">
                    <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">الإمارات العربية المتحدة</h5>
                    <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">حكومة الفجيرة</h5>
                    <h5 style="color:#000000; font-size: 16px;font-weight: 700;">نادي الفجيرة العلمي</h5>
                </div>
            </div>
            <div class="flex-fill text-center">
                <center>
                    <img src="~/ar/assets/images/logo-ar.png" alt="نادى الفجيره العلمى" style="width:75px;">
                </center>
            </div>
            <div class="d-flex justify-content-end align-items-center flex-fill">
                <div style="text-align:center">
                    <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">UNITED ARAB EMIRATES</h5>
                    <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">GOVT. OF FUJAIRAH</h5>
                    <h5 style="color:#000000; font-size: 16px;font-weight: 700;">FUJAIRAH SCIENCE CLUB</h5>
                </div>
            </div>
        </div>
        <div class="inner-container mt-3 mb-3">
            <div>
                <h1 class="text-center fs-4 pb-1 mb-2" style="color:#af1717">  طلب صرف  نقدى / شيكات / تحويل</h1>
                <h1 class="text-center fs-4 pb-1 mb-2" style="color:#af1717"> Cash Disbursing @Enum.GetName(typeof(CashDisbursementVoucherType), Model.Type ?? 0) / Check / Transfer</h1>
            </div>
            <div class="row gap-2 d-flex justify-content-between text-center">
                <div class="col-3 p-2">
                    <span class="mt-1">بتاريخ</span>
                    <div class=" border border-1 border-black  py-1 px-3 mt-2">
                        @Model.Date
                    </div>
                </div>
                <div class="col-3 p-2">
                    <div class=" border border-1 border-black  py-1 px-3 text-danger" style="color:#af1717  !important">
                        @Model.DocumentNo
                    </div>
                </div>
                <div class="col-3 p-2">
                    <span class="mt-1">لشهر</span>
                    <div class=" border border-1 border-black  py-1 px-3 mt-2">
                        @Model.Month
                    </div>
                </div>
            </div>
            <div>
                <textarea readonly class="w-100 my-2 p-2" rows="5">@Model.Description</textarea>
            </div>

            <!--Accountant Signature Section -->
            <div class="row  d-flex justify-content-end mt-3">
                <!-- Manager Signature (Activities/Membership Supervisor) -->
                <div class="col-3 text-center" style="margin-top:-20px;">
                    <label class="form-label fw-semibold text-black">@Resource1.Accountant : @Model?.AccountantFullName </label>
                    @if (Model?.DisbursementRequestAccountantSignature != null && !string.IsNullOrEmpty(Model.DisbursementRequestAccountantSignature.ImagePath))
                    {
                        <div class="small text-muted">
                            <img src="~/@Model.DisbursementRequestAccountantSignature.ImagePath" class="img-fluid " style="height: 70px;" alt="Accountant Signature" />
                        </div>
                    }
                    else if (PermissionScanner.ValidateRoleNumber() == RoleNumber.Accountant && await PermissionScanner.CheckLoggedUserIfHasSignature())
                    {
                        <div id="AccountantSignatureSection" class="signature-trigger" role="button" tabindex="0" style="cursor:pointer;">
                            <span>إضغط هنا للتوقيع</span>
                        </div>
                    }
                </div>
            </div>

            <div class="mb-3 w-50" style="margin: 0 auto 1.5rem auto !important;">
                <div class=" border border-1 border-black  text-center py-2">
                    @Resource2.AmountsToBeDisbursedFromClubAccount
                </div>
            </div>
            <div class="overflow-table">
                <table class="text-center table">
                    <thead>
                        <tr>
                            <th style="background-color:#eee;color:#000 !important">م</th>
                            <th style="background-color:#eee;color:#000 !important">@Resource2.ItemNumber</th>
                            <th style="background-color:#eee;color:#000 !important">@Resource2.DocumentDate</th>
                            <th style="background-color:#eee;color:#000 !important">@Resource1.Description</th>
                            <th class="red-bg" style="background-color:#af1717;color:#fff !important">@Resource2.Amount</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model?.Details != null)
                        {
                             var idx = 1; 
                            foreach (var detail in Model.Details)
                            {
                                <tr>
                                    <td style="width:7%;" ><input readonly type="number" value="@idx" style="border:0; text-align:center;" /></td>
                                    <td style="width:7%;"><input readonly type="number" value="@detail.DocumentNo" style="border:0; text-align:center;" /></td>
                                    <td style="width:7%;"><input readonly type="text" value="@detail.DocumentDate?.ToString("d")?.Replace("/","-")" style="border:0; text-align:center;" /></td>
                                    <td>
                                        <div style="white-space: normal; word-wrap: break-word;">
                                            @detail.Notes
                                        </div>
                                    </td>
                                    <td style="width:7%;"><input readonly type="number" min="0.01" value="@detail.Amount" style="border:0; text-align:center;" /></td>
                                </tr>
                                 idx++; 
                            }
                        }
                        <tr class="last-row" >
                            <td class="redtext" colspan="4" style="color:#af1717;background-color:#eee;font-weight:500;">
                                المجموع :  @TafqeetHelper.Tafqeet(Model?.TotalAmount ?? 0, CurrencyHelper.AED_Main_Ar, CurrencyHelper.AED_Sub_Ar)
                            </td>
                            <td class="red-bg" style="background-color:#af1717;color:#fff !important;  text-align:center;">
                                <input readonly type="number" value="@Model?.TotalAmount" style="color:#fff !important;  text-align:center; border: 0;
    background: transparent;" />
                            </td>
                        </tr>
                       
                    </tbody>
                </table>
            </div>
            <!-- Signature Section -->
            <div class="row  d-flex justify-content-end mt-3">
                <!-- Manager Signature (Activities/Membership Supervisor) -->
                <div class="col-3 text-center">
                    <label class="form-label fw-semibold text-black">
                        يعتمده
                        <span class="d-block">@Model?.ManagerFullName</span>
                    </label>
                    @if (Model?.DisbursementRequestAccountantSignature != null && !string.IsNullOrEmpty(Model.DisbursementRequestAccountantSignature.ImagePath))
                    {
                        @if (Model?.DisbursementRequestSignature != null && !string.IsNullOrEmpty(Model.DisbursementRequestSignature.ImagePath))
                        {
                            <div class="small text-muted">
                                <img src="~/@Model.DisbursementRequestSignature.ImagePath" class="img-fluid " style="height: 70px;" alt="Manager Signature" />
                            </div>
                        }
                        else if (PermissionScanner.ValidateRoleNumber() == RoleNumber.Manager && await PermissionScanner.CheckLoggedUserIfHasSignature())
                        {
                            <div id="managerSignatureSection" class="signature-trigger" role="button" tabindex="0" style="cursor:pointer;">
                                <span>إضغط هنا للتوقيع</span>
                            </div>
                        }
                    }

                </div>
            </div>
            <!-- OTP Modal: shared for both roles -->
            <div class="modal fade" id="otpModal" tabindex="-1" aria-labelledby="otpModalLabel" aria-hidden="true">
                <div class="modal-dialog modal-dialog-centered">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="otpModalLabel">@Resource2.EnterOTP</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                        </div>
                        <form id="otpForm">
                            <input type="hidden" id="otpEntityId" name="id" value="@Model?.Id" />
                            <input type="hidden" id="otpRole" name="role" value="" />
                            <div class="modal-body text-center">
                                <p>@Resource2.PleaseEnterOTPToConfirmSignature</p>
                                <div class="d-flex justify-content-center gap-2">
                                    @for (int i = 0; i < 4; i++)
                                    {
                                        <input inputmode="numeric" maxlength="1" class="form-control otp-digit text-center fs-4" style="width:60px;" />
                                    }
                                </div>
                                <div class="invalid-feedback text-center mt-2" style="display:none;">@Resource2.EnterValidDigitsCode</div>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn-cancel-search btn" data-bs-dismiss="modal">@Resource2.Close</button>
                                <button type="submit" class="btn btn-success">@Resource2.Confirm</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {

    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const otpInputs = Array.from(document.querySelectorAll("#otpModal .otp-digit")).reverse(); // reverse order for RTL
            const otpModal = document.getElementById("otpModal");
            const invalidFeedback = document.querySelector("#otpModal .invalid-feedback");

            otpInputs.forEach((input, index) => {
                input.addEventListener("input", (e) => {
                    e.target.value = e.target.value.replace(/\D/g, ""); // only digits
                    if (e.target.value && index < otpInputs.length - 1) {
                        otpInputs[index + 1].focus(); // move left (since reversed)
                    }
                });

                input.addEventListener("keydown", (e) => {
                    if (e.key === "Backspace" && !input.value && index > 0) {
                        otpInputs[index - 1].focus();
                    }
                });
            });

            otpModal.addEventListener("shown.bs.modal", () => {
                otpInputs.forEach(i => i.value = "");
                otpInputs[0].focus();
                invalidFeedback.style.display = "none";
            });

            document.getElementById("otpForm").addEventListener("submit", function (e) {
                const otpValue = otpInputs.map(i => i.value).reverse().join(""); // restore normal order
                if (otpValue.length < 4) {
                    e.preventDefault();
                    invalidFeedback.style.display = "block";
                    return false;
                }
                invalidFeedback.style.display = "none";
                console.log("OTP entered:", otpValue);
            });
        });
    </script>


    <script>
        $(document).ready(function () {
            function openOtpModal(role) {
                $('#otpRole').val(role);
                // Send OTP
                $.ajax({
                    url: '/Admin/CashDisbursementVoucher/SendOtp',
                    type: 'POST',
                    data: { id: $('#otpEntityId').val(), role: role },
                    success: function (response) {
                        if (response.success) {
                            const otpModal = new bootstrap.Modal(document.getElementById('otpModal'));
                            otpModal.show();
                        } else {
                            alert('Failed to send OTP.');
                        }
                    },
                    error: function () {
                        alert('Error sending OTP.');
                    }
                });
            }
                $('#managerSignatureSection').on('click', function () { openOtpModal('manager'); });
                $('#AccountantSignatureSection').on('click', function () { openOtpModal('accountant'); });

            // OTP form logic
            const otpModalEl = document.getElementById('otpModal');
            if (!otpModalEl) return;
            const otpModal = new bootstrap.Modal(otpModalEl, { backdrop: 'static', keyboard: false });
            const otpDigits = Array.from(otpModalEl.querySelectorAll('.otp-digit'));
            const otpForm = document.getElementById('otpForm');
            const otpInvalid = otpModalEl.querySelector('.invalid-feedback');

            otpForm.addEventListener('submit', async function (e) {
                e.preventDefault();
                const code = otpDigits.map(d => d.value.trim()).join('');
                const id = document.getElementById('otpEntityId')?.value;
                const role = document.getElementById('otpRole')?.value;

                if (!/^\d{4}$/.test(code)) {
                    otpDigits.forEach(d => d.classList.add('is-invalid'));
                    otpInvalid.style.display = 'block';
                    return;
                }

                otpDigits.forEach(d => d.classList.remove('is-invalid'));
                otpInvalid.style.display = 'none';

                try {
                    const response = await fetch('/Admin/CashDisbursementVoucher/CashDisbursementRequestValidateOtp', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ id: id, code: code, role: role })
                    });
                    const result = await response.json();

                    if (result.success) {
                        otpModal.hide();
                        // Optionally reload page or update signature section
                        location.reload();
                    } else {
                        otpDigits.forEach(d => d.classList.add('is-invalid'));
                        otpInvalid.textContent = result.message || "Invalid OTP.";
                        otpInvalid.style.display = 'block';
                    }
                } catch (err) {
                    alert('OTP validation failed.');
                }
            });
        });
    </script>

}
