@model ScientificProjectsVM

@{
    ViewData["Title"] = @Resource1.Details_ScientificProject;
    ViewData["Title pakage"] = @Resource1.ScientificProjectsManagement ?? " ";
    ViewData["Title ListIndex"] = @Resource1.ScientificProjectsList ?? " ";
    ViewData["Title Single"] = @Resource1.Details_ScientificProject ?? " ";

    var printDetailsPermission = PermissionScanner.ValidatePermission("ScientificProjects", "PrintDetails");

    var boolManagerSignature = Model.ManagerSignature != null && !string.IsNullOrEmpty(Model.ManagerSignature.ImagePath);
    var boolActivityMonitorSigniture = Model.ActivityMonitorSigniture != null && !string.IsNullOrEmpty(Model.ActivityMonitorSigniture.ImagePath);
    var boolTrainerSigniture = Model.Trainer1Signiture != null && !string.IsNullOrEmpty(Model.Trainer1Signiture.ImagePath);
}
  <script>
    function printDiv(divId) {
      window.print();
      document.body.innerHTML = originalPage;
      location.reload();
    }
</script>
<style>
    .form-label {
        margin-bottom: 0;}
    label {
        color: #fff;
        color: #fff !important;
        font-weight: 600;
        margin-bottom:0 !important;
    }

    .overflow-table {
        width: 100%;
        overflow-x: auto;
        -webkit-overflow-scrolling: touch;
    }

        .overflow-table::-webkit-scrollbar {
            height: 4px;
        }

        .overflow-table::-webkit-scrollbar-thumb {
            background-color: green;
            border-radius: 2px;
        }

        .overflow-table::-webkit-scrollbar-track {
            background-color: transparent;
        }

    .container {
        width: 100% !important;
        box-shadow: none !important;
        page-break-inside: avoid;
    }

    th, td {
        padding: 0;
        box-shadow: none;
        border: 1px #000 solid !important;
    }

    .form-control:disabled {
        border: 0;
        font-size: 15px;
        color: #000;
    }

    @@media print {
        html, body {
            width: 100%;
            overflow: visible !important;
        }

        .container {
            max-width: 100% !important;
        }

        img {
            max-width: 100% !important;
        }
        * {
            border: 0 !important;
            box-shadow: none !important;
        }

        .overflow-table {
            width: 100%;
            overflow-x: hidden;
        }

        .print-none,
        .custom-navbar {
            display: none !important;
        }

        body {
            background: #fff !important;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            font-size: 12px;
            margin: 0 !important;
            padding: 0 !important;
        }

        #printArea {
            width: 800px !important;
            margin: 0 auto !important;
            padding: 0 !important;
        }

        .row {
            margin-left: 0 !important;
            margin-right: 0 !important;
        }

        .flex-fill {
            flex: 1 1 auto !important;
            min-width: 0 !important;
        }

        table {
            outline: none !important;
            width: 100% !important;
            max-width: 100% !important;
            table-layout: fixed !important;
            box-shadow: none !important;
            border-collapse: collapse !important;
        }

        th, td {
            padding: 0;
            border: 1px solid #000 !important;
            padding: 1px !important;
        }

         
        input {
            border: none !important;
            box-shadow: none !important;
            background: transparent !important;
            -webkit-appearance: none;
        }
    }
</style>
<div class="row my-3 d-flex justify-content-between align-items-center flex-wrap print-none">
    <div class="col-md-6">
         <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-0 py-2 flex-nowrap">
                 <li class="breadcrumb-item">
                    <a href="#">@ViewData["Title pakage"]</a>
                </li>
                <li class="breadcrumb-item"><a href="/Admin/ScientificProjects/Index">@ViewData["Title ListIndex"]</a></li>
                <li class="breadcrumb-item">
                    <a href="#">@ViewData["Title Single"]</a>
                </li>
            </ol>
        </nav>
    </div>
    <div class="col-md-6">
        <div class="d-flex align-items-center justify-content-end gap-2">
            @if (boolManagerSignature && boolActivityMonitorSigniture && boolTrainerSigniture)
                    {   
                <button onclick="printDiv('printArea')" ;="" return="" false;"="" class="print-btn btn btn-success btn-sm">
                    <i class="fa fa-print me-1"></i>@Resource2.Print
                </button>
                    }
                    <a asp-action="Index" class="btn btn-cancel-search btn-sm back-btn">
                       @Resource2.Back <i class="fa-solid fa-arrow-left me-1"></i>
                    </a>
        </div>
    </div>
</div>

<div class="card">
  <div class="container" id="printArea">
       <div class="d-flex justify-content-between align-items-center flex-wrap border-black  border-bottom p-3  mb-3">
            <div class="d-flex justify-content-between align-items-center flex-fill">
                <div style="text-align:center">
                    <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">الإمارات العربية المتحدة</h5>
                    <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">حكومة الفجيرة</h5>
                    <h5 style="color:#000000; font-size: 16px;font-weight: 700;">نادي الفجيرة العلمي</h5>
                </div>
            </div>
            <div class="flex-fill text-center">
                <center>
                    <img src="~/ar/assets/images/logo-ar.png" alt="نادى الفجيره العلمى" style="width:75px;">
                </center>
            </div>
            <div class="d-flex justify-content-end align-items-center flex-fill">
                <div style="text-align:center">
                    <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">UNITED ARAB EMIRATES</h5>
                    <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">GOVT. OF FUJAIRAH</h5>
                    <h5 style="color:#000000; font-size: 16px;font-weight: 700;">FUJAIRAH SCIENCE CLUB</h5>
                </div>
            </div>
        </div>
        <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">
            <div class="d-flex align-items-center gap-2">
                <label class="form-label" asp-for="DateByCalander" style="color:#000 !important;">تاريخ</label>
                <input asp-for="DateByCalander" disabled hidden readonly type="date" class="form-control" />
                <input readonly type="text" disabled value="@Model?.DateByCalander?.ToString("d")?.Replace("/","-")" class="form-control" style="border-bottom:1px dashed #cccccc !important;text-align: center;" />
            </div>
            <div class="d-flex align-items-center gap-2">
                <label class="mb-0" style="color:#000 !important;">رقم</label>
                <input hidden type="hidden" asp-for="SerialCode" />
                <input disabled value="@Model?.SerialCode" readonly class="form-control " style="width: auto;border-bottom:1px dashed #cccccc !important; border-right:0 !important; border-left:0 !important; border-top:0 !important;text-align: center;">
            </div>
        </div>
        <table style="width:100%;  text-align:center;" cellspacing="0" cellpadding="5" class="mb-2">
            <caption style="font-weight:bold; font-size:18px;color:#af1717" class="text-center mb-2"> @Resource1.Details_ScientificProject</caption>

            <tr>
                <td style="width:20% !important;background-color:#af1717!important; color:#fff !important;font-weight: 600;">
                    <label class="form-label" asp-for="ProjectNameAr">@Resource1.ProjectNameAr</label>
                </td>
                <td style="width: 39.5%;"><input asp-for="ProjectNameAr" disabled readonly class="form-control" /></td>
                <td style="width:20% !important;background-color:#af1717!important; color:#fff !important;font-weight: 600;">رقم المشروع</td>
                <td style="width: 39.5%;">
                    <input hidden asp-for="SerialCode" />
                    <input disabled class="form-control" asp-for="SerialCode" readonly />
                </td>
            </tr>
            <tr>
                <td style="width:20% !important;background-color:#af1717!important; color:#fff !important;font-weight: 600;"> <label class="form-label" asp-for="IdeaOwnerAr">@Resource1.IdeaOwnerAr</label></td>
                <td style="width: 39.5%;">
                    <input asp-for="IdeaOwnerAr" disabled readonly class="form-control" />
                </td>
                <td style="width:20% !important;;background-color:#af1717!important; color:#fff !important;font-weight: 600;"><label class="form-label" asp-for="DepartmentId">@Resource2.ScientificSpecialization</label></td>
                <td style="width: 39.5%;">
                    <select asp-for="DepartmentId" disabled class="form-control">
                        <option value="">@Resource1.Select_Department</option>
                        @if (Model.all_Departments != null)
                        {
                            @if (SessionHelper.GetCurrentLanguage() == "ar")
                            {
                                foreach (var dept in Model.all_Departments)
                                {
                                    <option value="@dept.Id">@dept.NameAr</option>
                                }
                            }
                            else
                            {
                                foreach (var dept in Model.all_Departments)
                                {
                                    <option value="@dept.Id">@dept.NameEn</option>
                                }
                            }
                        }
                    </select>
                </td>
            </tr>
        </table>
        <table style="width:100%;  text-align:center;border-collapse:collapse;" cellspacing="0" cellpadding="5" class="mb-2">

            <tr>
                <td style="width:20%;background-color:#af1717!important; color:#fff !important;font-weight: 600;"> <label class="form-label" asp-for="ProjectIdeaAr">@Resource1.ProjectIdeaAr</label></td>
                <td colspan="2">
                    <textarea asp-for="ProjectIdeaAr" disabled readonly class="form-control" rows="1"></textarea>
                </td>
                @{
                    int visibleRows = 0;

                    if (!string.IsNullOrEmpty(Model.ProjectElement1) || !string.IsNullOrEmpty(Model.ProjectElement2)) visibleRows++;
                    if (!string.IsNullOrEmpty(Model.ProjectElement3) || !string.IsNullOrEmpty(Model.ProjectElement4)) visibleRows++;
                    if (!string.IsNullOrEmpty(Model.ProjectElement5) || !string.IsNullOrEmpty(Model.ProjectElement6)) visibleRows++;
                    if (!string.IsNullOrEmpty(Model.ProjectElement7) || !string.IsNullOrEmpty(Model.ProjectElement8)) visibleRows++;
                }

                @if (!string.IsNullOrEmpty(Model.ProjectElement1) || !string.IsNullOrEmpty(Model.ProjectElement2))
                {
                <tr>
                    <td rowspan="@visibleRows" style="width:20%;background-color:#af1717!important;color:#fff!important;font-weight:600;">
                        @Resource1.ProjectElements
                    </td>
                    <td style="width:40%;">
                        @if (!string.IsNullOrEmpty(Model.ProjectElement1))
                        {
                            <input asp-for="ProjectElement1" disabled readonly class="form-control" placeholder="@Resource1.Element 1" />
                        }
                    </td>
                    <td>
                        @if (!string.IsNullOrEmpty(Model.ProjectElement2))
                        {
                            <input asp-for="ProjectElement2" disabled readonly class="form-control" placeholder="@Resource1.Element 2" />
                        }
                    </td>
                </tr>
            }

            @if (!string.IsNullOrEmpty(Model.ProjectElement3) || !string.IsNullOrEmpty(Model.ProjectElement4))
            {
                <tr>
                    <td>
                        @if (!string.IsNullOrEmpty(Model.ProjectElement3))
                        {
                            <input asp-for="ProjectElement3" disabled readonly class="form-control" placeholder="@Resource1.Element 3" />
                        }
                    </td>
                    <td>
                        @if (!string.IsNullOrEmpty(Model.ProjectElement4))
                        {
                            <input asp-for="ProjectElement4" disabled readonly class="form-control" placeholder="@Resource1.Element 4" />
                        }
                    </td>
                </tr>
            }

            @if (!string.IsNullOrEmpty(Model.ProjectElement5) || !string.IsNullOrEmpty(Model.ProjectElement6))
            {
                <tr>
                    <td>
                        @if (!string.IsNullOrEmpty(Model.ProjectElement5))
                        {
                            <input asp-for="ProjectElement5" disabled readonly class="form-control" placeholder="@Resource1.Element 5" />
                        }
                    </td>
                    <td>
                        @if (!string.IsNullOrEmpty(Model.ProjectElement6))
                        {
                            <input asp-for="ProjectElement6" disabled readonly class="form-control" placeholder="@Resource1.Element 6" />
                        }
                    </td>
                </tr>
            }

            @if (!string.IsNullOrEmpty(Model.ProjectElement7) || !string.IsNullOrEmpty(Model.ProjectElement8))
            {
                <tr>
                    <td>
                        @if (!string.IsNullOrEmpty(Model.ProjectElement7))
                        {
                            <input asp-for="ProjectElement7" disabled readonly class="form-control" placeholder="@Resource1.Element 7" />
                        }
                    </td>
                    <td>
                        @if (!string.IsNullOrEmpty(Model.ProjectElement8))
                        {
                            <input asp-for="ProjectElement8" disabled readonly class="form-control" placeholder="@Resource1.Element 8" />
                        }
                    </td>
                </tr>
            }

        </table>
        <table style="width:100%;  text-align:center; border-collapse:collapse;" cellspacing="0" cellpadding="5" class="mb-2">
            <tr>
                <td style="width:20%;background-color:#af1717!important; color:#fff !important;font-weight: 600;"><label class="form-label" asp-for="InstallationRecommendations">@Resource1.InstallationAndConnections</label></td>
                <td colspan="2"><textarea asp-for="InstallationRecommendations" disabled readonly class="form-control" rows="2"></textarea></td>
            </tr>
            <tr>
                <td style="width:20%;background-color:#af1717!important; color:#fff !important;font-weight: 600;"><label class="form-label" asp-for="DeliveryData">@Resource2.DeliveryTechniques</label></td>
                <td colspan="2"><textarea asp-for="DeliveryData" disabled readonly class="form-control" rows="2"></textarea></td>
            </tr>
        </table>
        <table style="width:100%; text-align:center;" cellspacing="0" cellpadding="5" class="mb-3">
            @{
                int visibleRows2 = 0;

                if (!string.IsNullOrEmpty(Model.Participant1) || !string.IsNullOrEmpty(Model.Participant2)) visibleRows2++;
                if (!string.IsNullOrEmpty(Model.Participant3) || !string.IsNullOrEmpty(Model.Participant4)) visibleRows2++;
            }

            @if (!string.IsNullOrEmpty(Model.Participant1) || !string.IsNullOrEmpty(Model.Participant2))
            {
                <tr>
                    <td rowspan="@visibleRows2"
                        style="width:20%;background-color:#af1717!important;color:#fff!important;font-weight:600;">
                        @Resource2.ParticipantsProjectElements
                    </td>
                    <td style="width:39.5%;">
                        @if (!string.IsNullOrEmpty(Model.Participant1))
                        {
                            <input asp-for="Participant1" disabled readonly class="form-control"
                                   placeholder="@Resource1.Participant 1" />
                        }
                    </td>
                    <td style="width:39.5%;">
                        @if (!string.IsNullOrEmpty(Model.Participant2))
                        {
                            <input asp-for="Participant2" disabled readonly class="form-control"
                                   placeholder="@Resource1.Participant 2" />
                        }
                    </td>
                </tr>
            }

            @if (!string.IsNullOrEmpty(Model.Participant3) || !string.IsNullOrEmpty(Model.Participant4))
            {
                <tr>
                    <td style="width:39.5%;">
                        @if (!string.IsNullOrEmpty(Model.Participant3))
                        {
                            <input asp-for="Participant3" disabled readonly class="form-control"
                                   placeholder="@Resource1.Participant 3" />
                        }
                    </td>
                    <td style="width:39.5%;">
                        @if (!string.IsNullOrEmpty(Model.Participant4))
                        {
                            <input asp-for="Participant4" disabled readonly class="form-control"
                                   placeholder="@Resource1.Participant 4" />
                        }
                    </td>
                </tr>
            }
        </table>



        <table style="width:100%;  text-align:center; border-collapse:collapse;" cellspacing="0" cellpadding="5" class="mb-3">
            @{
                int supervisorRows = 0;
                if (!string.IsNullOrEmpty(Model.Supervisor1) || !string.IsNullOrEmpty(Model.Supervisor2)) supervisorRows++;
                if (!string.IsNullOrEmpty(Model.Supervisor3) || !string.IsNullOrEmpty(Model.Supervisor4)) supervisorRows++;
                if (!string.IsNullOrEmpty(Model.Supervisor5) || !string.IsNullOrEmpty(Model.Supervisor6)) supervisorRows++;
            }

            @if (supervisorRows > 0)
            {
                <tr>
                    <td rowspan="@supervisorRows"
                        style="width:20%;background-color:#af1717;color:#fff;font-weight:600;">
                        @Resource1.Supervisors
                    </td>

                    <td style="width:40%;">
                        @if (!string.IsNullOrEmpty(Model.Supervisor1))
                        {
                            <input asp-for="Supervisor1" class="form-control" disabled readonly />
                        }
                    </td>

                    <td style="width:40%;">
                        @if (!string.IsNullOrEmpty(Model.Supervisor2))
                        {
                            <input asp-for="Supervisor2" class="form-control" disabled readonly />
                        }
                    </td>
                </tr>
            }

            @if (!string.IsNullOrEmpty(Model.Supervisor3) || !string.IsNullOrEmpty(Model.Supervisor4))
            {
                <tr>
                    <td style="width:40%;">
                        @if (!string.IsNullOrEmpty(Model.Supervisor3))
                        {
                            <input asp-for="Supervisor3" class="form-control" disabled readonly />
                        }
                    </td>

                    <td style="width:40%;">
                        @if (!string.IsNullOrEmpty(Model.Supervisor4))
                        {
                            <input asp-for="Supervisor4" class="form-control" disabled readonly />
                        }
                    </td>
                </tr>
            }

            @if (!string.IsNullOrEmpty(Model.Supervisor5) || !string.IsNullOrEmpty(Model.Supervisor6))
            {
                <tr>
                    <td style="width:40%;">
                        @if (!string.IsNullOrEmpty(Model.Supervisor5))
                        {
                            <input asp-for="Supervisor5" class="form-control" disabled readonly />
                        }
                    </td>

                    <td style="width:40%;">
                        @if (!string.IsNullOrEmpty(Model.Supervisor6))
                        {
                            <input asp-for="Supervisor6" class="form-control" disabled readonly />
                        }
                    </td>
                </tr>
            }

            <tr>

                <td style="width:20%;background-color:#af1717!important; color:#fff !important;font-weight: 600;">@Resource1.ExpectedCost</td>
                <td colspan="2">
                    <input asp-for="ExpectedCost" disabled readonly type="number" class="form-control" />
                </td>
            </tr>
        </table>

        <table style="width:100%;  text-align:center; border-collapse:collapse;" cellspacing="0" cellpadding="5" class="mb-3">
            @* @{
            //     int SignatureRows = 0;
            //     SignatureRows = 3;
            //     // if (!string.IsNullOrEmpty(Model.Supervisor1) || !string.IsNullOrEmpty(Model.Supervisor2)) SignatureRows++;
            //     // if (!string.IsNullOrEmpty(Model.Supervisor3) || !string.IsNullOrEmpty(Model.Supervisor4)) SignatureRows++;
            //     // if (!string.IsNullOrEmpty(Model.Supervisor5) || !string.IsNullOrEmpty(Model.Supervisor6)) SignatureRows++;
            // }

            // @if (SignatureRows > 0)
            // {
            //     <tr>
            //         <td rowspan="@SignatureRows"
            //             style="width:20%;background-color:#af1717;color:#fff;font-weight:600;">
            //             إعتماد المشروع
            //         </td>

            //         <td style="width:40%;">
            //             @if (!string.IsNullOrEmpty(Model.Supervisor1))
            //             {
            //                 <input asp-for="Supervisor1" class="form-control" disabled readonly />
            //             }
            //         </td>

            //         <td style="width:40%;">
            //             @if (!string.IsNullOrEmpty(Model.Supervisor2))
            //             {
            //                 <input asp-for="Supervisor2" class="form-control" disabled readonly />
            //             }
            //         </td>
            //     </tr>
            // }*@


            <tr>
                <td rowspan="3"
                    style="width:20%;background-color:#af1717;color:#fff;font-weight:600;">
                    @Resource1.AdoptProject
                </td>

                <td style="width: 40%;">@Resource1.TrainerSignature : @Model.TrainerFullName</td>
                <td style="width: 40%;">

                    <div class="small text-muted">
                        @if (Model?.Trainer1Signiture != null && !string.IsNullOrEmpty(Model.Trainer1Signiture.ImagePath))
                        {
                            <img src="~/@Model.Trainer1Signiture.ImagePath" class="img-fluid" style="max-height: 70px;" alt="Manager Signature" />
                        }
                        else if (await PermissionScanner.CheckLoggedUserIfTrainer() && await PermissionScanner.CheckLoggedUserIfHasSignature())
                        {
                            <div id="TrainerSignatureSection" class="signature-trigger" role="button" tabindex="0" style="cursor:pointer;">
                                <span>إضغط هنا للتوقيع</span>
                            </div>
                        }
                    </div>
                </td>
            </tr>
                        
            <tr>

                <td style="width: 40%;">@Resource1.SupervisorSignature : @Model.SupervisorFullName</td>
                <td style="width: 40%;">

                    <div class="small text-muted">
                        @if (boolTrainerSigniture)
                        {
                            @if (Model?.ActivityMonitorSigniture != null && !string.IsNullOrEmpty(Model.ActivityMonitorSigniture.ImagePath))
                            {
                                <img src="~/@Model.ActivityMonitorSigniture.ImagePath" class="img-fluid" style="max-height: 70px;" alt="Manager Signature" />

                            }
                            else if (PermissionScanner.ValidateRoleNumber() == RoleNumber.ActivitiesSupervisor && await PermissionScanner.CheckLoggedUserIfHasSignature())
                            {
                                <div id="activityMonitorSignatureSection" class="signature-trigger" role="button" tabindex="0" style="cursor:pointer;">
                                    <span>إضغط هنا للتوقيع</span>
                                </div>
                            }
                        }
                    </div>
                </td>
            </tr>


            <tr>

                <td style="width: 40%;">@Resource1.ManagerSignature : @Model.ManagerFullName</td>
                <td style="width: 40%;">

                    <div class="small text-muted">
                        @if (boolActivityMonitorSigniture && boolTrainerSigniture)
                        {
                            @if (Model?.ManagerSignature != null && !string.IsNullOrEmpty(Model.ManagerSignature.ImagePath))
                            {
                                <img src="~/@Model.ManagerSignature.ImagePath" class="img-fluid" style="max-height: 70px;" alt="Manager Signature" />

                            }
                            else if (PermissionScanner.ValidateRoleNumber() == RoleNumber.Manager && await PermissionScanner.CheckLoggedUserIfHasSignature())
                            {
                                <div id="managerSignatureSection" class="signature-trigger" role="button" tabindex="0" style="cursor:pointer;">
                                    <span>إضغط هنا للتوقيع</span>
                                </div>
                            }
                        }
                    </div>
                </td>
            </tr>
        </table>

        <!-- OTP Modal: shared for both roles -->
        <div class="modal fade" id="otpModal" tabindex="-1" aria-labelledby="otpModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="otpModalLabel">@Resource2.EnterOTP</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                    </div>
                    <form id="otpForm">
                        <input type="hidden" id="otpEntityId" name="id" value="@Model?.Id" />
                        <input type="hidden" id="otpRole" name="role" value="" />
                        <div class="modal-body text-center">
                            <p>@Resource2.PleaseEnterOTPToConfirmSignature</p>
                            <div class="d-flex justify-content-center gap-2">
                                @for (int i = 0; i < 4; i++)
                                {
                                    <input inputmode="numeric" maxlength="1" class="form-control otp-digit text-center fs-4" style="width:60px;" />
                                }
                            </div>
                            <div class="invalid-feedback text-center mt-2" style="display:none;">@Resource2.EnterValidDigitsCode</div>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn-cancel-search btn" data-bs-dismiss="modal">@Resource2.Close</button>
                            <button type="submit" class="btn btn-success">@Resource2.Confirm</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    </div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const otpInputs = Array.from(document.querySelectorAll("#otpModal .otp-digit")).reverse(); // reverse order for RTL
            const otpModal = document.getElementById("otpModal");
            const invalidFeedback = document.querySelector("#otpModal .invalid-feedback");

            otpInputs.forEach((input, index) => {
                input.addEventListener("input", (e) => {
                    e.target.value = e.target.value.replace(/\D/g, ""); // only digits
                    if (e.target.value && index < otpInputs.length - 1) {
                        otpInputs[index + 1].focus(); // move left (since reversed)
                    }
                });

                input.addEventListener("keydown", (e) => {
                    if (e.key === "Backspace" && !input.value && index > 0) {
                        otpInputs[index - 1].focus();
                    }
                });
            });

            otpModal.addEventListener("shown.bs.modal", () => {
                otpInputs.forEach(i => i.value = "");
                otpInputs[0].focus();
                invalidFeedback.style.display = "none";
            });

            document.getElementById("otpForm").addEventListener("submit", function (e) {
                const otpValue = otpInputs.map(i => i.value).reverse().join(""); // restore normal order
                if (otpValue.length < 4) {
                    e.preventDefault();
                    invalidFeedback.style.display = "block";
                    return false;
                }
                invalidFeedback.style.display = "none";
                console.log("OTP entered:", otpValue);
            });
        });
    </script>

    <script>


        $(document).ready(function () {
            function openOtpModal(role) {
                $('#otpRole').val(role);
                // Send OTP
                $.ajax({
                    url: '/Admin/ScientificProjects/SendOtp',
                    type: 'POST',
                    data: { id: $('#otpEntityId').val(), role: role },
                    success: function (response) {
                        if (response.success) {
                            const otpModal = new bootstrap.Modal(document.getElementById('otpModal'));
                            otpModal.show();
                        } else {
                            alert('Failed to send OTP.');
                        }
                    },
                    error: function () {
                        alert('Error sending OTP.');
                    }
                });
            }
            $('#activityMonitorSignatureSection').on('click', function () { openOtpModal('activityMonitor'); });
            $('#managerSignatureSection').on('click', function () { openOtpModal('manager'); });
            $('#TrainerSignatureSection').on('click', function () { openOtpModal('Trainer'); });

            // OTP form logic
            const otpModalEl = document.getElementById('otpModal');
            if (!otpModalEl) return;
            const otpModal = new bootstrap.Modal(otpModalEl, { backdrop: 'static', keyboard: false });
            const otpDigits = Array.from(otpModalEl.querySelectorAll('.otp-digit'));
            const otpForm = document.getElementById('otpForm');
            const otpInvalid = otpModalEl.querySelector('.invalid-feedback');

            otpForm.addEventListener('submit', async function (e) {
                e.preventDefault();
                const code = otpDigits.map(d => d.value.trim()).join('');
                const id = document.getElementById('otpEntityId')?.value;
                const role = document.getElementById('otpRole')?.value;

                if (!/^\d{4}$/.test(code)) {
                    otpDigits.forEach(d => d.classList.add('is-invalid'));
                    otpInvalid.style.display = 'block';
                    return;
                }

                otpDigits.forEach(d => d.classList.remove('is-invalid'));
                otpInvalid.style.display = 'none';

                try {
                    const response = await fetch('/Admin/ScientificProjects/ValidateOtp', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ id: id, code: code, role: role })
                    });
                    const result = await response.json();

                    if (result.success) {
                        otpModal.hide();
                        // Optionally reload page or update signature section
                        location.reload();
                    } else {
                        otpDigits.forEach(d => d.classList.add('is-invalid'));
                        otpInvalid.textContent = result.message || "Invalid OTP.";
                        otpInvalid.style.display = 'block';
                    }
                } catch (err) {
                    alert('OTP validation failed.');
                }
            });
        });
    </script>
}
