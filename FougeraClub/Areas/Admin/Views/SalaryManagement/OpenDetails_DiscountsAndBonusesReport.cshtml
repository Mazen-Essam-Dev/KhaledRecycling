@using Domain.DTOs.Admin.SalaryManagement
@using Application.Helpers
@using Infrastructure.Identity
@inject UserManager<ApplicationUser> UserManager
@model SalaryReportVM

@{
    ViewData["Title"] = @Resource1.DiscountsAndBonusesReport ?? " ";
    ViewData["Title pakage"] = @Resource1.SalaryManagementUnitManagement ?? " ";
    ViewData["Title ListIndex"] = @Resource1.DiscountsAndBonusesReport ?? " ";
    ViewData["Title Single"] = @Resource2.Approval1 ?? " ";

    var allyears = ViewBag.allYears as List<int>;

    var AllEmployeesNames = ViewBag.EmployeesNames as IEnumerable<EmployeesNameDTO>;

    var lang = SessionHelper.GetCurrentLanguage();

    var addPermission = PermissionScanner.ValidatePermission("SalaryManagement", "Add");
    var editPermission = PermissionScanner.ValidatePermission("SalaryManagement", "Edit");
    var deletePermission = PermissionScanner.ValidatePermission("SalaryManagement", "Delete");

    var boolManagerSignature = Model?.ManagerSignature != null && !string.IsNullOrEmpty(Model.ManagerSignature.ImagePath);
    var boolAcountantSignature = Model?.AcountantSignature != null && !string.IsNullOrEmpty(Model.AcountantSignature.ImagePath);
    //var titleLable = (ViewBag.selectedYear != null ? Resource1.Summary + " " + Resource1.ReportThe + " " + Resource1.AdminstrativeThe + " " + QuarterName : " ");
    var m = Model?.month;
    var titleLable = m != null ? new DateTime(1, m ?? 1, 1).ToString("MMMM") + " - " + +Model?.year : " ";

    var acctUser = (Model?.AcountantSignature != null && !string.IsNullOrEmpty(Model.AcountantSignature.UserId)) ? await UserManager.FindByIdAsync(Model.AcountantSignature.UserId) : null;
    var mgrUser = (Model?.ManagerSignature != null && !string.IsNullOrEmpty(Model.ManagerSignature.UserId)) ? await UserManager.FindByIdAsync(Model.ManagerSignature.UserId) : null;
}
<style>
    .overflow-table {
        width: 100%;
        overflow-x: auto;
        -webkit-overflow-scrolling: touch;
        max-width: 100% !important;
    }

        .overflow-table::-webkit-scrollbar {
            height: 4px;
        }

        .overflow-table::-webkit-scrollbar-thumb {
            background-color: green;
            border-radius: 2px;
        }

        .overflow-table::-webkit-scrollbar-track {
            background-color: transparent;
        }

    .container {
        width: 100% !important;
        box-shadow: none !important;
        page-break-inside: avoid;
    }

    @@page {
        size: A4 landscape;
        margin: 10mm;
    }

    th, td {
        box-shadow: none;
        box-shadow: inset 0 0.25px 0 0 #000, inset 0 -0.25px 0 0 #000, inset 0.25px 0 0 0 #000, inset -0.25px 0 0 0 #000;
        border: 0 !important;
    }

    @@media print {
        * {
            max-width: 100% !important;
        }

        html, body {
            width: 100%;
            overflow: visible !important;
            margin: 0 !important;
            padding: 0 !important;
        }

        .overflow-table {
            width: 100%;
            max-width: 100%;
            overflow-x: hidden;
        }

        .print-none,
        .custom-navbar {
            display: none !important;
        }

        body {
            background: #fff !important;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            font-size: 12px;
            margin: 0 !important;
            padding: 0 !important;
        }

        #printArea {
            width: 100% !important;
            max-width: 100% !important;
            margin: 0 auto !important;
            padding: 0 !important;
        }

        .row {
            margin-left: 0 !important;
            margin-right: 0 !important;
        }

        .flex-fill {
            flex: 1 1 auto !important;
            min-width: 0 !important;
        }

        table {
            outline: none !important;
            width: 100% !important;
            max-width: 100% !important;
            box-shadow: none !important;
            table-layout: fixed !important;
            border-spacing: 0 !important;
            border-collapse: collapse !important;
        }

            table th,
            table td {
                word-wrap: break-word !important;
                white-space: normal !important;
                padding: 1px !important;
                box-shadow: inset 0 0.25px 0 0 #000, inset 0 -0.25px 0 0 #000, inset 0.25px 0 0 0 #000, inset -0.25px 0 0 0 #000;
            }



        input {
            border: none !important;
            box-shadow: none !important;
            background: transparent !important;
            -webkit-appearance: none;
        }
    }
</style>
<div class="row my-3 d-flex justify-content-between align-items-center flex-wrap print-none">
    <div class="col-md-6">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-0 py-2 flex-nowrap">
                <li class="breadcrumb-item">
                    <a href="#">@ViewData["Title pakage"]</a>
                </li>
                <li class="breadcrumb-item"><a href="/Admin/SalaryManagement/Index">@ViewData["Title ListIndex"]</a></li>

                <li class="breadcrumb-item">
                    <a href="#">@ViewData["Title Single"]</a>
                </li>
            </ol>
        </nav>
    </div>
    <div class="col-md-6">
        <div class="d-flex align-items-center justify-content-end gap-2">
            @if (boolManagerSignature)
            {
                <button onclick="printDiv('printArea')" ;="" return="" false;"="" class="print-btn btn btn-success btn-sm">
                    <i class="fa-solid fa-print me-1"></i> @Resource1.Print
                </button>
            }
            <input type="hidden" hidden id="selectedYear" name="selectedYear" value="@ViewBag.SelectedYear" />
            <input type="hidden" hidden id="quarter" name="quarter" value="@ViewBag.quarter" />
            <a class="btn btn-cancel-search btn-sm back-btn" asp-action="Index">
                @Resource2.Back  <i class="fa-solid fa-arrow-left me-1"></i>
            </a>
        </div>
    </div>
</div>
<input hidden asp-for="IsAbleToOpen" id="IsAbleToOpen" />

<div class="card">
    <div class="container mb-3" id="printArea">
        <div class="print-header">
            <div class="d-flex justify-content-between align-items-center flex-wrap  border-bottom p-3  mb-3">
                <div class="d-flex justify-content-between align-items-center flex-fill">
                    <div style="text-align:center">
                        <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">الإمارات العربية المتحدة</h5>
                        <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">حكومة الفجيرة</h5>
                        <h5 style="color:#000000; font-size: 16px;font-weight: 700;">نادي الفجيرة العلمي</h5>
                    </div>
                </div>
                <div class="flex-fill text-center">
                    <center>
                        <img src="/ar/assets/images/logo-ar.png" alt="نادى الفجيره العلمى" style="width:75px;">
                    </center>
                </div>
                <div class="d-flex justify-content-end align-items-center flex-fill">
                    <div style="text-align:center">
                        <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">UNITED ARAB EMIRATES</h5>
                        <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">GOVT. OF FUJAIRAH</h5>
                        <h5 style="color:#000000; font-size: 16px;font-weight: 700;">FUJAIRAH SCIENCE CLUB</h5>
                    </div>
                </div>
            </div>
        </div>

        <div style="color:#af1717; font-weight:bold; font-size:17px; text-align:center; margin-bottom:30px;">
            @ViewData["Title"] @* @titleLable *@
        </div>
        <div>
            <!-- Table -->
            <div class="overflow-table">
                <!-- Table -->
                <div class="table-responsive">
                    <table class="table table-bordered">
                        <thead style="background-color:#47A644; color: white;">
                            <tr>
                                <th style="min-width: 200px !important;" colspan="3">@Resource1.Name</th>
                                <th style="min-width: 200px !important;" colspan="3">@Resource1.JobTitle1</th>
                                <th style="min-width: 90px !important;">@Resource1.BasicSalary</th>
                                <th style="min-width: 50px !important;">@Resource1.Allowances</th>
                                <th style="min-width: 50px !important;">@Resource1.TotalDeductions</th>
                                <th style="min-width: 50px !important;">@Resource1.BonusesAndMissions</th>
                                <th style="min-width: 50px !important;">@Resource1.NetSalary</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model != null && Model.salaryManagementVMs != null && Model.salaryManagementVMs.Any())
                            {
                                lang = SessionHelper.GetCurrentLanguage();

                                // Show only records where TotalDeductions or BonusesAndMissions are non-zero
                                var filteredSalaryManagementVMs = Model.salaryManagementVMs
                                    .Where(s => (s.TotalDeductions.HasValue && s.TotalDeductions.Value != 0m) 
                                             || (s.BonusesAndMissions.HasValue && s.BonusesAndMissions.Value != 0m))
                                    .ToList();

                                if (filteredSalaryManagementVMs.Any())
                                {
                                    foreach (var item in filteredSalaryManagementVMs)
                                    {
                                        DateTime dateMonth = new DateTime(2025, item.Month, 1);
                                        <tr>
                                            <td colspan="3">
                                                @(lang == "ar" ? AllEmployeesNames?.Where(y => y.Id ==
                                                                                item.EmployeeId)?.FirstOrDefault()?.FullNameAr : AllEmployeesNames?.Where(y => y.Id ==
                                                                                item.EmployeeId)?.FirstOrDefault()?.FullNameEn)
                                            </td>
                                            <td colspan="3">@item.JobTitle</td>
                                            
                                            <td>@((item.BasicSalary == null || item.BasicSalary == 0) ? "0" : item.BasicSalary)</td>
                                            <td>@((item.Allowances == null || item.Allowances == 0) ? "0" : item.Allowances)</td>
                                            <td>@((item.TotalDeductions == null || item.TotalDeductions == 0) ? "0" : item.TotalDeductions)</td>
                                            <td>@((item.BonusesAndMissions == null || item.BonusesAndMissions == 0) ? "0" : item.BonusesAndMissions)</td>
                                            <td>@((item.NetSalary == null || item.NetSalary == 0) ? "0" : item.NetSalary)</td>

                                        </tr>
                                    }
                                   
                                    var BasicSalary = filteredSalaryManagementVMs.Sum(y => y.BasicSalary ?? 0m);
                                    var Allowances = filteredSalaryManagementVMs.Sum(y => y.Allowances ?? 0m);
                                    var TotalDeductions = filteredSalaryManagementVMs.Sum(y => y.TotalDeductions ?? 0m);
                                    var BonusesAndMissions = filteredSalaryManagementVMs.Sum(y => y.BonusesAndMissions ?? 0m);
                                    var NetSalary = filteredSalaryManagementVMs.Sum(y => y.NetSalary ?? 0m);
                                                      
                                    
                                    <tr class="last-tr">
                                        <td colspan="3">@Resource1.Total1</td>
                                        <td colspan="3"> </td>
                                        <td>@((BasicSalary == 0 || BasicSalary == null) ? "0" : BasicSalary.ToString("F2"))</td>
                                        <td>@((Allowances == 0 || Allowances == null) ? "0" : Allowances.ToString("F2"))</td>
                                        <td>@((TotalDeductions == 0 || TotalDeductions == null) ? "0" : TotalDeductions.ToString("F2"))</td>
                                        <td>@((BonusesAndMissions == 0 || BonusesAndMissions == null) ? "0" : BonusesAndMissions.ToString("F2"))</td>
                                        <td>@((NetSalary == 0 || NetSalary == null) ? "0" : NetSalary.ToString("F2"))</td>

                                    </tr>
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="11" class="text-center p-5">
                                            <div class="d-flex flex-column align-items-center justify-content-center text-muted">
                                                <i class="ri-alert-line" style="font-size: 3rem;"></i>
                                                <h5 class="mt-3">@Resource1.NoData</h5>
                                                <p class="mb-0">@Resource2.GoToCreatePage</p>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="11" class="text-center p-5">
                                        <div class="d-flex flex-column align-items-center justify-content-center text-muted">
                                            <i class="ri-alert-line" style="font-size: 3rem;"></i>
                                            <h5 class="mt-3">@Resource1.NoData</h5>
                                            <p class="mb-0">@Resource2.GoToCreatePage</p>
                                        </div>
                                    </td>
                                </tr>

                            }
                        </tbody>

                        <tfoot>
                            <tr style="border: none;">
                                <td colspan="2" style="min-height:300px !important;border: none !important;">
                                    <div class="footer-container" id="footer-container" style="color: rgba(0, 0, 0, 0) !important;">
                                        <div class="footer-top" id="footer-top">
                                            <div class="right">Time: </div>
                                            <div class="center" style="color: rgba(0, 0, 0, 0) !important;">Date: </div>
                                            <div class="left">Printed by: </div>
                                        </div>

                                        <div class="footer-bottom" id="footer-bottom">
                                            @Resource1.FujairahPrintFooter © @AppDubaiTime.Now.ToString("yyyy")
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        </tfoot>

                    </table>
                </div>
            </div>
        </div>
        <div class="d-flex justify-content-end border-black border-top" style="padding-top: 20px;">
            <div class="d-flex justify-content-between w-100">
                <div class="flex-column align-items-center flex-column text-center" style="width:50%;">
                    <label class="form-label fw-bold text-black">
                        <span>@Resource1.Accountant : @(lang == "ar" ? acctUser?.FullNameAr ?? acctUser?.Email : acctUser?.FullNameEn ?? acctUser?.Email)</span>
                    </label>
                    <div class="small text-muted">
                        @if (Model?.AcountantSignature != null && !string.IsNullOrEmpty(Model.AcountantSignature.ImagePath))
                        {
                            <img src="~/@Model.AcountantSignature.ImagePath" class="img-fluid"
                                 style="height:70px;" />
                          
                        }
                        else if (PermissionScanner.ValidateRoleNumber() == RoleNumber.Accountant
                        && await PermissionScanner.CheckLoggedUserIfHasSignature())
                        {
                            <div id="AcountantSignatureSection" class="signature-trigger" role="button" tabindex="0"
                                 style="cursor:pointer;">
                                <span>إضغط هنا للتوقيع</span>
                            </div>
                        }
                    </div>
                </div>
                <div class="flex-column align-items-center flex-column text-center" style="width:50%;">
                    <label class="form-label fw-bold text-black">
                        <span>@Resource1.SignatureManager : @(lang == "ar" ? mgrUser?.FullNameAr ?? mgrUser?.Email : mgrUser?.FullNameEn ?? mgrUser?.Email)</span>
                    </label>

                    <div class="small text-muted">
                        @if (Model?.ManagerSignature != null && !string.IsNullOrEmpty(Model.ManagerSignature.ImagePath))
                        {
                            <img src="~/@Model.ManagerSignature.ImagePath" class="img-fluid" style="height:70px;" />
                        }
                        else if (PermissionScanner.ValidateRoleNumber() == RoleNumber.Manager
                            && await PermissionScanner.CheckLoggedUserIfHasSignature())
                        {
                            <div id="managerSignatureSection" class="signature-trigger" role="button" tabindex="0" style="cursor:pointer;">
                                <span>إضغط هنا للتوقيع</span>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
        <!-- OTP Modal -->
        <div class="modal fade" id="otpModal" tabindex="-1" aria-labelledby="otpModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="otpModalLabel">@Resource2.EnterOTP</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                    </div>
                    <form id="otpForm">
                        <input type="hidden" id="otpYear" name="year" value="@Model?.year" />
                        <input type="hidden" id="otpMonth" name="month" value="@Model?.month" />
                        <input type="hidden" id="otpRole" name="role" value="" />

                        <div class="modal-body text-center">
                            <p>@Resource2.PleaseEnterOTPToConfirmSignature</p>

                            <div class="d-flex justify-content-center gap-2">
                                @for (int i = 0; i < 4; i++)
                                {
                                    <input inputmode="numeric" maxlength="1" class="form-control otp-digit text-center fs-4"
                                           style="width:60px;" />
                                }
                            </div>

                            <div class="invalid-feedback text-center mt-2" style="display:none;">
                                @Resource2.EnterValidDigitsCode
                            </div>
                        </div>

                        <div class="modal-footer">
                            <button type="button" class="btn-cancel-search btn" data-bs-dismiss="modal">
                                @Resource2.Close
                            </button>
                            <button type="submit" class="btn btn-success">@Resource2.Confirm</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

    </div>
</div>


@section Scripts {

    <script>
        function printDiv(divId) {
            window.print();
            document.body.innerHTML = originalPage;
            location.reload();
        }
    </script>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const otpInputs = Array.from(document.querySelectorAll("#otpModal .otp-digit")).reverse(); // reverse order for RTL
            const otpModal = document.getElementById("otpModal");
            const invalidFeedback = document.querySelector("#otpModal .invalid-feedback");

            otpInputs.forEach((input, index) => {
                input.addEventListener("input", (e) => {
                    e.target.value = e.target.value.replace(/\D/g, ""); // only digits
                    if (e.target.value && index < otpInputs.length - 1) {
                        otpInputs[index + 1].focus(); // move left (since reversed)
                    }
                });

                input.addEventListener("keydown", (e) => {
                    if (e.key === "Backspace" && !input.value && index > 0) {
                        otpInputs[index - 1].focus();
                    }
                });
            });

            otpModal.addEventListener("shown.bs.modal", () => {
                otpInputs.forEach(i => i.value = "");
                otpInputs[0].focus();
                invalidFeedback.style.display = "none";
            });

            document.getElementById("otpForm").addEventListener("submit", function (e) {
                const otpValue = otpInputs.map(i => i.value).reverse().join(""); // restore normal order
                if (otpValue.length < 4) {
                    e.preventDefault();
                    invalidFeedback.style.display = "block";
                    return false;
                }
                invalidFeedback.style.display = "none";
                console.log("OTP entered:", otpValue);
            });
        });
    </script>

  

  

  

   

        <script>
            // Toaster For you can't Print
            function showToastrCannotPrintPleseSelectFirst() {
              toastr.options = {
                "closeButton": true,
                "progressBar": true,
                "positionClass": "toast-top-center",
                "timeOut": "3000"
              };
              var cantPrint2 ='@Html.Raw(Resource1.PleaseChooseYearAndMonthFirstly)';

              toastr.error(cantPrint2);
            }
        </script>

    <script>
        $(document).ready(function () {
            function openOtpModal(role) {
                $('#otpRole').val(role);
                // Send OTP
                $.ajax({
                    url: '/Admin/SalaryManagement/SendOtp',
                    type: 'POST',
                    data: { year: $('#otpYear').val(), month: $('#otpMonth').val(), role: role },
                    success: function (response) {
                        if (response.success) {
                            bootstrap.Modal.getOrCreateInstance(document.getElementById('otpModal')).show();
                        } else {
                            alert('Failed to send OTP.');
                        }
                    },
                    error: function () {
                        alert('Error sending OTP.');
                    }
                });
            }
            $('#AcountantSignatureSection').on('click', function () { openOtpModal('Acountant'); });
            $('#managerSignatureSection').on('click', function () { openOtpModal('manager'); });

            // OTP form logic
            const otpModalEl = document.getElementById('otpModal');
            if (!otpModalEl) return;
            const otpModal = bootstrap.Modal.getOrCreateInstance(otpModalEl, { backdrop: 'static', keyboard: false });
            const otpDigits = Array.from(otpModalEl.querySelectorAll('.otp-digit'));
            const otpForm = document.getElementById('otpForm');
            const otpInvalid = otpModalEl.querySelector('.invalid-feedback');

            otpForm.addEventListener('submit', async function (e) {
                e.preventDefault();
                const code = otpDigits.map(d => d.value.trim()).join('');
                const year = document.getElementById('otpYear')?.value;
                const month = document.getElementById('otpMonth')?.value;
                const role = document.getElementById('otpRole')?.value;

                if (!/^\d{4}$/.test(code)) {
                    otpDigits.forEach(d => d.classList.add('is-invalid'));
                    otpInvalid.style.display = 'block';
                    return;
                }

                otpDigits.forEach(d => d.classList.remove('is-invalid'));
                otpInvalid.style.display = 'none';

                try {
                    const response = await fetch('/Admin/SalaryManagement/ValidateOtp_OpenDetails_DiscountsAndBonusesReport', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ year: parseInt(year), month: parseInt(month), code: code, role: role })
                    });
                    const result = await response.json();

                    if (result.success) {
                        otpModal.hide();
                        location.reload();
                    } else {
                        otpDigits.forEach(d => d.classList.add('is-invalid'));
                        otpInvalid.textContent = result.message || "Invalid OTP.";
                        otpInvalid.style.display = 'block';
                    }
                } catch (err) {
                    alert('OTP validation failed.');
                }
            });
        });
    </script>
}
 