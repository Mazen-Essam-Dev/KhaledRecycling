@using FougeraClub.Areas.Admin.ViewModels.MaintenanceClub
@model PaginatedList<MaintenanceClubVM>

@{
    ViewData["Title"] = @Resource1.ClubMaintenanceManagementList ?? " ";
    ViewData["Title pakage"] = @Resource1.ClubMaintenanceManagement ?? " ";
    ViewData["Title Single"] = @Resource1.ClubMaintenanceManagementList ?? " ";
    var addPermission = PermissionScanner.ValidatePermission("MaintenanceClub", "Add");
    var editPermission = PermissionScanner.ValidatePermission("MaintenanceClub", "Edit");
    var deletePermission = PermissionScanner.ValidatePermission("MaintenanceClub", "Delete");
}



<!-- Header with Title, Count, and Create Button -->
<div class="row my-3 d-flex justify-content-between align-items-center flex-wrap">
    <!-- Breadcrumb Navigation Column -->
    <div class="col-md-6">
        <nav aria-label="breadcrumb" class="ms-2">
            <ol class="breadcrumb mb-0 py-2 flex-nowrap">
                <li class="breadcrumb-item">
                    <a href="#">@ViewData["Title pakage"]</a>
                </li>
                <li class="breadcrumb-item">
                    <a href="#">@ViewData["Title Single"]</a>
                </li>
            </ol>
        </nav>
    </div>

    <!-- Records Count and Action Buttons -->
    <div class="col-md-6 d-flex align-items-center gap-2 flex-wrap justify-content-end">
        <div class="records-count-box bg-opacity-10 rounded border-opacity-25 h-auto">
            <span>@Resource1.RecordsCount:</span>
            <span id="countRecords">@ViewBag.TotalCount</span>
        </div>
    </div>

    <script>
        function openPrintPage() {
            const get = id => document.getElementById(id)?.value || '';
            const searchTerm = encodeURIComponent(get('searchTermInput'));
            const dateFrom = encodeURIComponent(get('dateFrom'));
            const dateTo = encodeURIComponent(get('dateTo'));
            const countRecords = document.getElementById('NewCountRecords')?.value;
            if (countRecords && countRecords > 3999) {
                showToastrCannotPrint();
            } else {
                const url = `/Admin/MaintenanceClub/Print?searchTerm=${searchTerm}&dateFrom=${dateFrom}&dateTo=${dateTo}`;
                window.open(url, 'printWindow', 'width=1000,height=800,noopener,noreferrer');
            }
        }
        function openExcelPage() {
            const get = id => document.getElementById(id)?.value || '';
            const searchTerm = encodeURIComponent(get('searchTermInput'));
            const dateFrom = encodeURIComponent(get('dateFrom'));
            const dateTo = encodeURIComponent(get('dateTo'));
            const url = `/Admin/MaintenanceClub/createExcelReport_Download?searchTerm=${searchTerm}&dateFrom=${dateFrom}&dateTo=${dateTo}`;
            window.open(url, '_self');
        }
    </script>
</div>
<div class="row">
    <div class="col-12">
        <div class="card">
            <!-- Header with actions + status badges -->
            <div class="card-header border-bottom card-tabs d-flex flex-wrap align-items-start gap-3">
                <div class="flex-grow-1">
                    <div class="d-flex align-items-center gap-2">
                        <h4 class="ico-header-title my-1 ms-lg-1 ms-md-1">
                            <span class="fa-solid fa-hotelme-1 text-ico"></span>
                            @Resource1.MaintenanceList
                        </h4>
                    </div>
                </div>
                <!-- Right: actions -->
                <div class="d-flex align-items-center gap-2 flex-wrap" style="margin-left: 4px;">
                    @if (addPermission)
                    {
                        <a asp-action="AddEdit" class="btn btn-outline-success btn-sm">
                            <i class="fa fa-plus me-1"></i> @Resource1.AddMaintenanceStatement
                        </a>
                    }
                    <button type="button" class="btn btn-success btn-sm" onclick="openPrintPage()">
                        <i class="fa fa-print me-1"></i> @Resource2.Print
                    </button>
                    <!-- Print Export As Excel -->
                    <a class="btn btn-success Excelbtn" onclick="openExcelPage();">
                        <i class="fa-solid fa-file-excel me-2"
                            style="font-size:17px;"></i>@Resource1.createExcelReport_Download
                    </a>
                </div>
            </div>
            <div class="rounded shadow-sm p-3">
                <form method="get" id="maintenanceClubFilterForm" class="filterImg rounded p-3">
                    <!-- Row 1: From & To -->
                    <div class="row">
                        <div class="col-md-4">
                            <label for="searchTermInput" class="fw-medium mb-2">@Resource2.Search:</label>
                            <input type="text" name="searchTerm" id="searchTermInput" value="@Model.SearchTerm"
                                   class="form-control green-border form-control-sm filter-bordered"
                                   placeholder="@Resource1.MaintenanceTitle" />
                        </div>
                        <div class="col-md-4">
                            <label for="dateFrom" class="fw-medium mb-2">@Resource1.DateFrom:</label>
                            <div class="input-icon-left">
                                <img src="https://cdn-icons-png.flaticon.com/512/747/747310.png" alt="calendar icon" class="date-icon">
                                <input type="date" name="dateFrom" id="dateFrom"
                                    class="form-control green-border form-control-sm filter-bordered"
                                    value="@(Model.dateFrom)" />
                            </div>
                        </div>
                        <div class="col-md-4">
                            <label for="dateTo" class="fw-medium mb-2">@Resource1.DateTo:</label>
                            <div class="input-icon-left">
                                <img src="https://cdn-icons-png.flaticon.com/512/747/747310.png" alt="calendar icon" class="date-icon">
                                <input type="date" name="dateTo" id="dateTo"
                                    class="form-control green-border form-control-sm filter-bordered"
                                    value="@(Model.dateTo)" />
                            </div>
                        </div>

                                         </div>
                    <!-- Row 3: Buttons -->
                    <div class="row">
                        <div class="col d-flex justify-content-end gap-2 mt-3">
                            <button type="button" id="btnClearFilters" class="btn-cancel-search btn-sm btn">
                                <i class="fa-solid fa-xmark me-1"></i> @Resource2.Clear
                            </button>
                            <button type="submit" class="btn btn-success btn-sm">
                                <i class="ri-filter-line me-1"></i> @Resource1.Search
                            </button>
                        </div>
                    </div>
                </form>


                <!-- Table -->
                <div id="PartialNewListContainer">
                    @await Html.PartialAsync("_ListPartial", Model)
                </div>

            </div>
        </div>
    </div>
</div>


<!-- Attachments Modal -->
<div class="modal fade" id="maintenanceClubAttachmentsModal" tabindex="-1" aria-hidden="true">
    <!-- Content loaded via AJAX -->
</div>

<!-- Delete Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">@Resource1.DeleteMaintenance</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                @Resource1.are_you_sure_Delete <strong id="itemName"></strong>
            </div>
            <div class="modal-footer">
                <form id="deleteForm" asp-action="Delete" method="post">
                    <input type="hidden" name="id" id="itemId" />
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">@Resource1.Cancel</button>
                    <button type="submit" class="btn btn-danger">@Resource1.YesDelete</button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        var deleteModal = document.getElementById('deleteModal');
        deleteModal.addEventListener('show.bs.modal', function (event) {
            var button = event.relatedTarget;
            var id = button.getAttribute('data-id');
            var name = button.getAttribute('data-name');
            document.getElementById('itemId').value = id;
            document.getElementById('itemName').textContent = name;
        });
    </script>

    <script>
        $(document).on("click", ".show-delete-info", function () {
            var message = $(this).data("message"); // take message from data-attribute
            $("#deleteInfoMessage").text(message);

            var modal = new bootstrap.Modal(document.getElementById("deleteInfoModal"));
            modal.show();
        });

    </script>

    <script>
        // Open Maintenance Club Attachments Modal
        $(document).on('click', '.open-maintenance-club-attachments-modal', function () {
            var maintenanceClubId = $(this).data('maintenance-club-id');

            $.ajax({
                url: '/Admin/MaintenanceClub/GetMaintenanceClubAttachments',
                type: 'GET',
                data: { maintenanceClubId: maintenanceClubId },
                success: function (data) {
                    $('#maintenanceClubAttachmentsModal').html(data);
                    $('#maintenanceClubAttachmentsModal').modal('show');
                },
                error: function () {
                    alert('Error loading attachments');
                }
            });
        });
    </script>

    <script>
        $(document).ready(function () {

            // We specify the storage key with the page name
            const pageKey = window.location.pathname + "_filters";

            // Retrieve stored filters when the page is opened
            if (sessionStorage.getItem(pageKey)) {
                let filters = JSON.parse(sessionStorage.getItem(pageKey));
                $('#searchTermInput').val(filters.searchTerm);
                $('#dateFrom').val(filters.dateFrom);
                $('#dateTo').val(filters.dateTo);
                $('#pageSizeSelect').val(filters.pageSize);

                // Load the list again with the old parameters
                loadNewPartialList(filters.page, filters.pageSize);
            }

            // When he click the search button
            $('form.filterImg').on('submit', function (e) {
                e.preventDefault();
                saveFilters(1); // We store from the first page
                loadNewPartialList(1);
            });

            // Clear Button
            $(document).on('click', '#btnClearFilters', function () {
                $('#searchTermInput').val('');
                $('#dateFrom').val('');
                $('#dateTo').val('');
                saveFilters(1);
                loadNewPartialList(1);
            });

            // Change the pagenation
            $(document).on('click', '.ajax-page', function () {
                var page = $(this).data('page');
                saveFilters(page);
                loadNewPartialList(page);
            });

            // Change the number of rows
            $(document).on('change', '#pageSizeSelect', function () {
                var pageSize = $(this).val();
                saveFilters(1, pageSize);
                loadNewPartialList(1, pageSize);
            });

            // function For Save The Last state
            function saveFilters(page, pageSizeOverride) {
                var filters = {
                    searchTerm: $('#searchTermInput').val(),
                    dateFrom: $('#dateFrom').val(),
                    dateTo: $('#dateTo').val(),
                    page: page,
                    pageSize: pageSizeOverride || $('#pageSizeSelect').val()
                };
                sessionStorage.setItem(pageKey, JSON.stringify(filters));
            }

            function loadNewPartialList(page, pageSizeOverride) {
                var searchTerm = $('#searchTermInput').val();
                var dateFrom = $('#dateFrom').val();
                var dateTo = $('#dateTo').val();
                var pageSize = pageSizeOverride || $('#pageSizeSelect').val();

                $.ajax({
                    url: '/Admin/MaintenanceClub/Index',
                    type: 'GET',
                    data: { searchTerm, dateFrom, dateTo, page, pageSize },
                    beforeSend: function () {
                        $('#PartialNewListContainer').html('<div class="text-center p-5">Loading...</div>');
                    },
                    success: function (data) {
                        $('#PartialNewListContainer').html(data);
                        const newCountRecords = $('#NewCountRecords').val();
                        $('#countRecords').text(newCountRecords);
                        initTooltips(); // this Function for Reload Tootips Text after Ajax Called in index Pages or another page
                    },
                    error: function () {
                        $('#PartialNewListContainer').html('<div class="alert alert-danger">Error loading data</div>');
                    }
                });
            }
        });
    </script>

}