@using Domain.DTOs.Admin.SalaryManagement
@using FougeraClub.Areas.Admin.ViewModels.ExternalWorkMission
@model ExternalWorkMissionVM
@{
    ViewData["Title"] = Resource1.ViewdetailsExternalworkMission1;
    ViewData["Title pakage"] = @Resource1.MissionsManagement ?? " ";
    ViewData["Title ListIndex"] = @Resource1.ExternalWorkMissionList ?? " ";
    ViewData["Title Single"] = Resource1.ViewdetailsExternalworkMission;

    var AllEmployeesNames = ViewBag.EmployeesNames as IEnumerable<EmployeesNameDTO>;
    var AllMissionsChecked = Model?.Missions.Select(x => x.MessionKey).Distinct().ToList();

    var boolManagerSignature = Model?.Signature != null && !string.IsNullOrEmpty(Model.Signature.ImagePath);
}
<script>
    function printDiv(divId) {
        window.print();
        document.body.innerHTML = originalPage;
        location.reload();
    }
</script>
<style>
    .overflow-table {
        width: 100%;
        overflow-x: auto;
        -webkit-overflow-scrolling: touch;
        max-width: 100% !important;
    }

    .overflow-table::-webkit-scrollbar {
        height: 4px;
    }

    .overflow-table::-webkit-scrollbar-thumb {
        background-color: green;
        border-radius: 2px;
    }

    .overflow-table::-webkit-scrollbar-track {
        background-color: transparent;
    }

    .container {
        width: 100% !important;
        box-shadow: none !important;
        page-break-inside: avoid;
    }

    th,
    td {
        box-shadow: none;
        border: 1px #000 solid !important;
    }

    @@media print {
        .form-check-input:checked[type=checkbox] {
            background-image: none !important;
            position: relative;
            width: 16px;
            height: 16px;  
            border: 1px solid #000;
            background-color: #fff;
        }

            .form-check-input:checked[type=checkbox]::after {
                content: "\2713";
                color: black;
                font-size: 10px;
                font-weight: bold;
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                pointer-events: none;
            }
        html,
        body {
            width: 100%;
            overflow: visible !important;
            margin: 0 !important;
            padding: 0 !important;
        }

        .overflow-table {
            width: 100%;
            max-width: 100%;
            overflow-x: hidden;
        }

        .print-none,
        .custom-navbar {
            display: none !important;
        }

        body {
            background: #fff !important;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            font-size: 12px;
            margin: 0 !important;
            padding: 0 !important;
        }

        #printArea {
            width: 800px !important;
            margin: 0 auto !important;
            padding: 0 !important;
        }

        .row {
            margin-left: 0 !important;
            margin-right: 0 !important;
        }

        .flex-fill {
            flex: 1 1 auto !important;
            min-width: 0 !important;
        }

        table {
            outline: none !important;
            width: 100% !important;
            max-width: 100% !important;
            box-shadow: none !important;
            border-collapse: collapse !important;
            table-layout: fixed !important;
            border-collapse: collapse !important;
        }



        table th,
        table td {
            word-wrap: break-word !important;
            white-space: normal !important;
            border: 1px solid #000 !important;
            padding: 0px !important;
        }

        * {
            max-width: 100% !important;
        }

        input {
            border: none !important;
            box-shadow: none !important;
            background: transparent !important;
            -webkit-appearance: none;
        }

        input[type="checkbox"] {
            appearance: auto !important;
            -webkit-appearance: auto !important;
            print-color-adjust: exact;
        }


        .form-check-input {
            width: 12px !important;
            height: 12px !important;
            transform: scale(1) !important;
            margin-top: 2px !important;
        }


    }
</style>
<div class="row my-3 d-flex justify-content-between align-items-center flex-wrap print-none">
    <div class="col-md-6">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-0 py-2 flex-nowrap">
                <li class="breadcrumb-item">
                    <a href="#">@ViewData["Title pakage"]</a>
                </li>

                <li class="breadcrumb-item"> <a href="/Admin/ExternalWorkMission/Index">@ViewData["Title ListIndex"]</a>
                </li>

                <li class="breadcrumb-item">
                    <a href="#">@ViewData["Title Single"]</a>
                </li>
            </ol>
        </nav>
    </div>
    <div class="col-md-6">
        <div class="d-flex align-items-center justify-content-end gap-2">
            @if (boolManagerSignature == true)
            {
                <button onclick="printDiv('printArea')" ;="" return="" false;"="" class="print-btn btn btn-success btn-sm">
                    <i class="fa fa-print me-1"></i> @Resource2.Print
                </button>
            }
            <a class="btn btn-cancel-search btn-sm back-btn" href="/Admin/ExternalWorkMission">
                @Resource1.Back <i class="fa-solid fa-arrow-left me-1"></i>
            </a>
        </div>
    </div>
</div>
<div class="card">
    <div class="container  mb-3" id="printArea">
        <div class="d-flex justify-content-between align-items-center flex-wrap border-black  border-bottom py-3  mb-3">
            <div class="d-flex justify-content-between align-items-center flex-fill">
                <div style="text-align:center">
                    <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">الإمارات العربية المتحدة</h5>
                    <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">حكومة الفجيرة</h5>
                    <h5 style="color:#000000; font-size: 16px;font-weight: 700;">نادي الفجيرة العلمي</h5>
                </div>
            </div>
            <div class="flex-fill text-center">
                <center>
                    <img src="/ar/assets/images/logo-ar.png" alt="نادى الفجيره العلمى" style="width:75px;">
                </center>
            </div>
            <div class="d-flex justify-content-end align-items-center flex-fill">
                <div style="text-align:center">
                    <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">UNITED ARAB EMIRATES</h5>
                    <h5 style="color:#6d6058; font-size: 16px;font-weight: 700;">GOVT. OF FUJAIRAH</h5>
                    <h5 style="color:#000000; font-size: 16px;font-weight: 700;">FUJAIRAH SCIENCE CLUB</h5>
                </div>
            </div>
        </div>
        <div class="text-center">
            <h5 class="fw-bold fs-4" style="color:#af1717">مهمة عمل خارجية / داخلية</h5>
        </div>
        <div class="inner-container mt-3 mb-3">
            <input asp-for="Id" hidden />
            <div class="overflow-table">
                <table style="width:100%;  text-align:center;border-collapse:collapse;" cellspacing="0" cellpadding="5"
                    class="mb-1">
                    <tr>
                        <td colspan="1" style="width:20%; background:#af1717; color:#fff;"><label asp-for="MissionDate"
                                class="form-label mb-0" style="color:#fff; font-weight:bold;">@Resource1.Date</label>
                        </td>
                        <td colspan="3" style="width:80%;">
                            <div class="input-icon-left">
                                <input id="ddate" value="@Model?.MissionDate?.ToString("d")?.Replace("/","-")" class="form-control form-control-sm border-0" type="text"
                                       readonly>
                            </div>
                            <span asp-validation-for="MissionDate" class="text-danger small"></span>
                        </td>
                    </tr>
                    <tr>
                        <td colspan="1" style="width:20%; background:#af1717; color:#fff;"><label asp-for="EmployeeId"
                                class="form-label mb-0"
                                style="color:#fff; font-weight:bold;">@Resource1.Employee</label></td>
                        <td colspan="3" style="width:80%;">
                            <input asp-for="EmployeeId" hidden type="hidden" />
                            @if (SessionHelper.GetCurrentLanguage() == "ar")
                            {
                                <input id="EmployeeId2" name="EmployeeId2"
                                    value="@AllEmployeesNames?.Where(x => x.Id == Model?.EmployeeId)?.FirstOrDefault()?.FullNameAr"
                                    readonly class="form-control border-0" />
                            }
                            else
                            {
                                <input id="EmployeeId2" name="EmployeeId2"
                                    value="@AllEmployeesNames?.Where(x => x.Id == Model?.EmployeeId)?.FirstOrDefault()?.FullNameEn"
                                    readonly class="form-control border-0" />
                            }
                            <span asp-validation-for="EmployeeId" class="text-danger small"></span>
                        </td>
                    </tr>
                    <tr>
                        <td colspan="1" style="background:#af1717; width:20%">
                            <label for="job" class="form-label mb-0"
                                style="color:#fff; font-weight:bold;">@Resource1.Job</label>
                        </td>
                        <td colspan="3" style="width:80%;">
                            <input id="job" name="job" value="@ViewBag.selectedEmployeeJob" readonly
                                class="form-control border-0" />
                        </td>
                    </tr>
                    <tr>
                        <td colspan="1" style="background:#af1717; width:20%"><label asp-for="MissionLocation"
                                class="form-label mb-0"
                                style="color:#fff; font-weight:bold;">@Resource1.MissionLocation</label></td>
                        <td colspan="3" style="width:80%">
                            <input asp-for="MissionLocation" readonly class="form-control border-0" />
                            <span asp-validation-for="MissionLocation" class="text-danger small"></span>
                        </td>
                    </tr>
                    <tr>
                        <td colspan="1" style="background:#af1717; width:20% "><label asp-for="MissionCountry"
                                class="form-label mb-0"
                                style="color:#fff; font-weight:bold;">@Resource1.MissionCountry</label></td>
                        <td colspan="3" style="width:80%">
                            <input asp-for="MissionCountry" readonly class="form-control border-0" />
                            <span asp-validation-for="MissionCountry" class="text-danger small"></span>
                        </td>
                    </tr>
                    <tr>
                        <td colspan="1" style="background:#af1717; "> <label asp-for="MissionWorkDescription"
                                class="form-label mb-0"
                                style="color:#fff; font-weight:bold;">@Resource1.MissionWorkDescription</label></td>
                        <td colspan="3" style="width:80%;">
                            <textarea asp-for="MissionWorkDescription" readonly class="form-control border-0"
                                rows="3"></textarea>
                            <span asp-validation-for="MissionWorkDescription" class="text-danger small"></span>
                        </td>
                    </tr>
                </table>
            </div>
            <div class="overflow-table">
                <table style="width:100%;  text-align:center;border-collapse:collapse;" cellspacing="0" cellpadding="5"
                    class="mb-1">
                    <tr>
                        <td colspan="4" style="height:30px;  background:#eee; font-weight:bold;">
                            @Resource1.MissionDetails</td>
                    </tr>
                    <tr>
                        <td colspan="1" style="width:20%; background:#eee;">
                            <label class="form-check-label mt-1 form-label mb-0 text-black"
                                style="font-weight:600;">@Resource1.MissionType</label>
                        </td>
                        <td colspan="3" style="width:80%; ">
                            <div class="col-md-12 d-flex flex-wrap gap-3 align-items-center px-2">

                                @if (AllMissionsChecked?.Count > 0)
                                {
                                    @if (AllMissionsChecked.Contains((int)MissionType.Forum))
                                    {

                                        <div class="form-check">
                                            <input id="MT1" class="form-check-input" type="checkbox" disabled checked="checked"
                                                name="MissionTypes" value="@((int)MissionType.Forum)"
                                                style=" background-color: #000 !important; border-color: #000 !important;" />
                                            <label for="MT1" class="form-check-label text-black">@Resource1.Forum</label>
                                        </div>
                                    }
                                    @if (AllMissionsChecked.Contains((int)MissionType.Conference))
                                    {
                                        <div class="form-check">
                                            <input id="MT2" class="form-check-input " type="checkbox" disabled checked="checked"
                                                name="MissionTypes" value="@((int)MissionType.Conference)"
                                                style=" background-color: #000 !important; border-color: #000 !important;" />
                                            <label for="MT2" class="form-check-label text-black">@Resource1.Conference</label>
                                        </div>
                                    }
                                    @if (AllMissionsChecked.Contains((int)MissionType.Exhibition))
                                    {
                                        <div class="form-check">
                                            <input id="MT3" class="form-check-input" type="checkbox" disabled checked="checked"
                                                name="MissionTypes" value="@((int)MissionType.Exhibition)"
                                                style=" background-color: #000 !important; border-color: #000 !important;" />
                                            <label for="MT3" class="form-check-label text-black">@Resource1.Exhibition</label>
                                        </div>
                                    }
                                    @if (AllMissionsChecked.Contains((int)MissionType.Competition))
                                    {
                                        <div class="form-check">
                                            <input id="MT4" class="form-check-input" type="checkbox" disabled checked="checked"
                                                name="MissionTypes" value="@((int)MissionType.Competition)"
                                                style=" background-color: #000 !important; border-color: #000 !important;" />
                                            <label for="MT4" class="form-check-label text-black">@Resource1.Competition</label>
                                        </div>
                                    }
                                    @if (AllMissionsChecked.Contains((int)MissionType.ClubEquipment))
                                    {
                                        <div class="form-check">
                                            <input id="MT5" class="form-check-input" type="checkbox" disabled checked="checked"
                                                name="MissionTypes" value="@((int)MissionType.ClubEquipment)"
                                                style=" background-color: #000 !important; border-color: #000 !important;" />
                                            <label for="MT5"
                                                class="form-check-label text-black">@Resource1.ClubEquipment</label>
                                        </div>
                                    }
                                    @if (AllMissionsChecked.Contains((int)MissionType.ExchangeOrDelivery))
                                    {
                                        <div class="form-check">
                                            <input id="MT6" class="form-check-input" type="checkbox" disabled checked="checked"
                                                name="MissionTypes" value="@((int)MissionType.ExchangeOrDelivery)"
                                                style=" background-color: #000 !important; border-color: #000 !important;" />
                                            <label for="MT6"
                                                class="form-check-label text-black">@Resource1.ExchangeOrDelivery</label>
                                        </div>
                                    }
                                }
                            </div>
                        </td>
                    </tr>
                </table>
            </div>
            <div class="overflow-table">
                @{
                    // Ensure null values are treated as 0
                    decimal petroleum = Model?.PetroleumFees ?? 0;
                    decimal food = Model?.FoodFees ?? 0;
                    decimal mission = Model?.MissionAllowance ?? 0;

                    decimal total = petroleum + food + mission;
                }

                <table style="width:100%; text-align:center; border-collapse:collapse;" cellspacing="0" cellpadding="5"
                    class="mb-1">
                    <tr>
                        <td colspan="4" style="height:30px; background:#eee; font-weight:bold;">@Resource1.RequiredFees
                        </td>
                    </tr>

                    <tr>
                        <td colspan="1" style="width:20%; background:#eee;">
                            <label asp-for="PetroleumFees" class="form-label mb-0 text-black"
                                style="font-weight:600;">@Resource1.PetroleumFees</label>
                        </td>
                        <td colspan="3" style="width:80%;">
                            <input asp-for="PetroleumFees" placeholder="0" readonly type="number"
                                class="form-control border-0" style="width:100%;" />
                            <span asp-validation-for="PetroleumFees" class="text-danger small"></span>
                        </td>
                    </tr>

                    <tr>
                        <td colspan="1" style="width:20%; background:#eee;">
                            <label asp-for="FoodFees" class="form-label mb-0 text-black"
                                style="font-weight:600;">@Resource1.FoodFees</label>
                        </td>
                        <td colspan="3" style="width:80%;">
                            <input asp-for="FoodFees" placeholder="0" type="number" readonly
                                class="form-control border-0" style="width:100%;" />
                            <span asp-validation-for="FoodFees" class="text-danger small"></span>
                        </td>
                    </tr>

                    <tr>
                        <td colspan="1" style="width:20%; background:#eee;">
                            <label asp-for="MissionAllowance" class="form-label mb-0 text-black"
                                style="font-weight:600;">@Resource1.MissionAllowance</label>
                        </td>
                        <td colspan="3" style="width:80%;">
                            <input asp-for="MissionAllowance" placeholder="0" type="number" readonly
                                class="form-control border-0" style="width:100%;" />
                            <span asp-validation-for="MissionAllowance" class="text-danger small"></span>
                        </td>
                    </tr>

                    <!-- Total Row -->
                    <tr>
                        <td colspan="1" style="width:20%; background:#eee;">
                            <label class="form-label mb-0 text-black" style="font-weight:600;">الإجمالى</label>
                        </td>
                        <td colspan="3" style="width:80%;">
                            <input value="@total" readonly type="number" class="form-control border-0"
                                style="width:100%;" />
                        </td>
                    </tr>
                </table>

            </div>
            <div class="overflow-table">
                <table style="width:100%; text-align:center; border-collapse:collapse;" cellspacing="0" cellpadding="5"
                    class="mb-1">
                    <tbody>
                        <!-- Header Row -->
                        <tr>
                            <td colspan="5" style="height:30px;background:#eee; font-weight:bold; color:#000">المرشحون
                                للمشاركة</td>
                        </tr>

                        <!-- Table Column Headers -->
                        <tr>
                            <td rowspan="5"
                                style="width:20%;background-color:#eee !important; color:#000 !important;font-weight:600;">
                                عدد الأشخاص
                            </td>
                        </tr>

                        @{
                            // Create a list dynamically
                            var candidates = new List<(string Name, string Adj)>
                                                {
                                                (Model?.CandidateName1, Model?.CandidateAdj1),
                                                (Model?.CandidateName2, Model?.CandidateAdj2),
                                                (Model?.CandidateName3, Model?.CandidateAdj3),
                                                (Model?.CandidateName4, Model?.CandidateAdj4)
                                                };

                            // Filter only rows that have actual values
                            var filledCandidates = candidates
                            .Where(c => !string.IsNullOrEmpty(c.Name) || !string.IsNullOrEmpty(c.Adj))
                            .ToList();
                        }

                        @foreach (var c in filledCandidates)
                        {
                            <tr>
                                <td>@c.Name</td>
                                <td colspan="3">@c.Adj</td>
                            </tr>
                        }
                    </tbody>
                </table>

            </div>
            <div class="overflow-table">
                <table style="width:100%;  text-align:center;border-collapse:collapse;" cellspacing="0" cellpadding="5"
                    class="mb-1">
                    <tr>
                        <td colspan="1"
                            style=" font-weight:bold; width:20%; background:#eee; font-weight:bold; color:#000">
                            <label asp-for="FoodFees" class="form-label mb-0 text-black"
                                style="font-weight:600;">@Resource1.Notes</label>
                        </td>
                        <td colspan="4">
                            <textarea asp-for="MissionWorkDescription" readonly class="form-control border-0"
                                rows="3"></textarea>
                            <span asp-validation-for="MissionWorkDescription" class="text-danger small"></span>
                        </td>
                    </tr>
                </table>
            </div>
            <!-- Signature Section -->
            <div class="d-flex justify-content-end border-black border-top mt-2">
                <div class="flex-column align-items-center" style="padding-top:10px;">
                    <label class="form-label mb-0 fw-bold text-black">@Resource1.SignatureManager / <span>
                            @Model.ManagerFullName
                        </span>
                    </label>
                    @if (Model?.Signature != null)
                    {
                        <div class="signature-trigger" role="button" tabindex="0" style="cursor:pointer;">
                            <div class="small text-muted">
                                @if (!string.IsNullOrEmpty(Model?.Signature.ImagePath))
                                {
                                    <div
                                        style=" height: 70px !important; overflow: hidden !important;page-break-inside: avoid !important; ">
                                        <img src="~/@Model?.Signature.ImagePath" class="img-fluid"
                                            style="object-fit: contain !important; max-height: 70px !important;width: auto !important;"
                                            alt="Upload one" />
                                       
                                    </div>
                                }
                            </div>
                        </div>
                    }
                    else if (PermissionScanner.ValidateRoleNumber() == RoleNumber.Manager && await
                    PermissionScanner.CheckLoggedUserIfHasSignature())
                    {
                        <div id="signatureSection" class="signature-trigger" role="button" tabindex="0"
                            style="cursor:pointer;">
                            <div class="small text-muted">إضغط هنا للتوقيع</div>
                        </div>
                    }
                </div>
            </div>
        </div>
        <!-- OTP Modal -->
        <div class="modal fade" id="otpModal" tabindex="-1" aria-labelledby="otpModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="otpModalLabel">@Resource2.EnterOTP</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                    </div>
                    <form id="otpForm">
                        <input type="hidden" id="otpEntityId" name="id" value="@Model?.Id" />
                        <div class="modal-body text-center">
                            <p>@Resource2.PleaseEnterOTPToConfirmSignature</p>
                            <div class="d-flex justify-content-center gap-2">
                                @for (int i = 0; i < 4; i++)
                                {
                                    <input inputmode="numeric" maxlength="1" class="form-control otp-digit text-center fs-4"
                                        style="width:60px;" />
                                }
                            </div>
                            <div class="invalid-feedback text-center mt-2" style="display:none;">
                                Enter a valid
                                4-digit code.
                            </div>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn-cancel-search btn" data-bs-dismiss="modal">
                                رجوع

                            </button>
                            <button type="submit" class="btn btn-success">تأكيد</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const otpInputs = Array.from(document.querySelectorAll("#otpModal .otp-digit")).reverse(); // reverse order for RTL
            const otpModal = document.getElementById("otpModal");
            const invalidFeedback = document.querySelector("#otpModal .invalid-feedback");

            otpInputs.forEach((input, index) => {
                input.addEventListener("input", (e) => {
                    e.target.value = e.target.value.replace(/\D/g, ""); // only digits
                    if (e.target.value && index < otpInputs.length - 1) {
                        otpInputs[index + 1].focus(); // move left (since reversed)
                    }
                });

                input.addEventListener("keydown", (e) => {
                    if (e.key === "Backspace" && !input.value && index > 0) {
                        otpInputs[index - 1].focus();
                    }
                });
            });

            otpModal.addEventListener("shown.bs.modal", () => {
                otpInputs.forEach(i => i.value = "");
                otpInputs[0].focus();
                invalidFeedback.style.display = "none";
            });

            document.getElementById("otpForm").addEventListener("submit", function (e) {
                const otpValue = otpInputs.map(i => i.value).reverse().join(""); // restore normal order
                if (otpValue.length < 4) {
                    e.preventDefault();
                    invalidFeedback.style.display = "block";
                    return false;
                }
                invalidFeedback.style.display = "none";
                console.log("OTP entered:", otpValue);
            });
        });
    </script>

    <script>
        $(document).ready(function () {
            $('#signatureSection').on('click', function () {
                $.ajax({
                    url: '/Admin/ExternalWorkMission/SendOtp',
                    type: 'POST',
                    success: function (response) {
                        if (response.success) {
                            bootstrap.Modal.getOrCreateInstance(document.getElementById('otpModal')).show();
                        } else {
                            console.warn('Failed to send OTP.');
                        }
                    },
                    error: function (xhr, status, error) {
                        console.error('Error sending OTP:', error);
                    }
                });
            });
        });
    </script>
    <script>
        (function () {
            const signature = document.getElementById('signatureSection');
            const otpModalEl = document.getElementById('otpModal');
            if (!otpModalEl) return;
            const otpModal = bootstrap.Modal.getOrCreateInstance(otpModalEl, { backdrop: 'static', keyboard: false });
            const otpDigits = Array.from(otpModalEl.querySelectorAll('.otp-digit'));
            const otpForm = document.getElementById('otpForm');
            const otpInvalid = otpModalEl.querySelector('.invalid-feedback');

            if (signature) {
                signature.addEventListener('click', () => otpModal.show());
                signature.addEventListener('keypress', e => {
                    if (e.key === 'Enter' || e.key === ' ') { otpModal.show(); e.preventDefault(); }
                });
            }

            otpForm.addEventListener('submit', async e => {
                e.preventDefault();
                const code = otpDigits.map(d => d.value.trim()).join('');
                const id = document.getElementById('otpEntityId')?.value;

                if (!/^\d{4}$/.test(code)) {
                    otpDigits.forEach(d => d.classList.add('is-invalid'));
                    otpInvalid.style.display = 'block';
                    return;
                }

                otpDigits.forEach(d => d.classList.remove('is-invalid'));
                otpInvalid.style.display = 'none';

                try {
                    const response = await fetch('@Url.Action("ValidateOtp", "ExternalWorkMission", new { area = "Admin" })', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ id: id, code: code })
                    });
                    const result = await response.json();

                    if (result.success) {
                        otpModal.hide();
                        location.reload();
                    } else {
                        otpDigits.forEach(d => d.classList.add('is-invalid'));
                        otpInvalid.textContent = result.message || "Invalid OTP.";
                        otpInvalid.style.display = 'block';
                    }
                } catch (err) {
                    console.error('OTP validation failed:', err);
                }
            });
        })();
    </script>
}